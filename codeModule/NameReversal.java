import java.util.Scanner;
public class NameReversal {
    public static void main (String [] args){
//        Scanner keyboard = new Scanner(System.in);
//        System.out.print("Enter any name to reverse: ");
//        String userInput = keyboard.next();

        String userInput = "ebube";
        String lastReverse = "";
        for (int minLoop = userInput.length() - 1; minLoop >= 0; minLoop--) {
            System.out.print(userInput.charAt(minLoop));
           if (userInput == userInput);
        }
//        String userInput = "Hello Ebube";
//        String lastReverse = "";
//        for (int minLoop = userInput.length() - 1; minLoop >= 0; minLoop--){
//            System.out.print(userInput.charAt(minLoop));
//        }

//        for (int minLoop = userInput.length() - 1; minLoop >= 0; minLoop--) {
//            lastReverse += userInput.charAt(minLoop);
//            System.out.print(lastReverse);
//        }
    }
}
